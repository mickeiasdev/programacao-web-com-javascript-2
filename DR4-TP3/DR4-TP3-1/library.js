function maxLength(item, num) {
    if (item.length > num) {
        return false
    } else {
        return true
    }
}

function minLength(item, num) {
    if (item.length < num) {
        return false
    } else {
        return true
    }
}