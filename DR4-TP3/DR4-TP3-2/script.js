//Exercício 2 Funções de Ordenação - Parte 1

// Enunciado: Crie uma aplicação que implemente uma biblioteca com 2 funções: sortNumberAsc e sortNumberDesc,
// recebendo um array como parâmetro. Crie um array com 10 números e use esse array para testar as duas funções,
// exibindo o resultado em 2 alerts separados e sequenciais. 

// Observações:

// Utilize alert.
// Implemente os métodos de forma a não alterar o array original.
// Use o operador de spread.

const arr = [1, 8, 6, 4, 10, 3, 7, 2, 9, 5];

ordenarCrescente(...arr);
ordenarDecrescente(...arr);